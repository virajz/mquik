<?php

use App\Modules\{{ studlyName }}\Livewire\Index;
use Illuminate\Support\Facades\Route;

Route::middleware(['auth', 'verified'])->group(function () {
    Route::get('/{{ kebabName }}', Index::class)->name('{{ kebabName }}.index');
});
